# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'stringutils' do
  developer('Erwin J. van Eijk', 'eijk@oakhill.nl')

  self.rubyforge_name = 'stringutils' # if different than 'stringutils'
end

# vim: syntax=ruby
